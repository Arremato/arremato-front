---
export interface FinancialData {
  totalValuation: number;
  totalExpenses: number;
  totalIncome: number;
  projectedProfit: number;
}

const financialData: FinancialData = {
  totalValuation: 1600000,
  totalExpenses: 500000,
  totalIncome: 200000,
  projectedProfit: 300000,
};
---

<div class="p-4 bg-white shadow rounded">
  <h3 class="text-lg font-bold mb-4">Visão Geral Financeira</h3>
  <ul class="space-y-2">
    <li><strong>Valor Total da Carteira:</strong> R$ {financialData.totalValuation.toLocaleString('pt-BR')}</li>
    <li><strong>Total de Despesas:</strong> R$ {financialData.totalExpenses.toLocaleString('pt-BR')}</li>
    <li><strong>Total de Receitas:</strong> R$ {financialData.totalIncome.toLocaleString('pt-BR')}</li>
    <li><strong>Lucro Projetado:</strong> R$ {financialData.projectedProfit.toLocaleString('pt-BR')}</li>
  </ul>
</div>